name: bscs
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  webview_flutter: ^4.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.6

flutter_launcher_icons:
  android: true
  ios: false
  image_path: lib/asstes/logo.png
  min_sdk_android: 21

flutter_native_splash:
  color: "#FFFFFF"
  image: lib/asstes/logo.png
  android: true
  ios: false
  web: false

flutter:
  uses-material-design: true
